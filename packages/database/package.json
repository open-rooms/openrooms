{
  "name": "@openrooms/database",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "@openrooms/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "prisma": "^5.8.0",
    "typescript": "^5.3.3"
  }
}
