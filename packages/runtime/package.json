{
  "name": "@openrooms/runtime",
  "version": "0.4.0",
  "description": "OpenRooms Stage 4 execution runtime — agent runner, workflow runner, tool executor, event bus, run manager",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@openrooms/core": "workspace:*",
    "@openrooms/database": "workspace:*",
    "bullmq": "^5.0.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  }
}
