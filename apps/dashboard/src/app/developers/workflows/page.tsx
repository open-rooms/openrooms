import { SubPageShell } from '@/components/SubPageShell'
export default function Page() {
  return <SubPageShell path="developers" title="Workflows" description="Build multi-agent pipelines with branching logic, parallel nodes, and chained steps. Workflows define the orchestration graph that the runtime executes." accentColor="#F54E00" textOnAccent="#fff" actionLabel="Build Workflow" actionHref="/workflows" />
}
