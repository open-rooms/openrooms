# ── Required: set this in Railway service variables ────────────────────────
# Point to your deployed API service URL (from Railway API service → Settings → Public Domain)
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# ── Optional ───────────────────────────────────────────────────────────────
NODE_ENV=production
